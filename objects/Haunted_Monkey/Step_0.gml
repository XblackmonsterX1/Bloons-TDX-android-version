target = noone;
targeting_scr(targeting_mode)
if target != noone
if (distance_to_object(Bloon)<range)
{
image_angle = (point_direction(x,y,other.target.x,other.target.y));
}

if speed = 0
if distance_to_object(Path) < 10
{
speed = 0
}

if (distance_to_object(Path)<32)
{
friction = 2;
}

